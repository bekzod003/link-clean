package service

type TagStorage interface {
	// Create(tag *entity.Tag) error
	// Get(id int) (*Tag, error)
	// GetByName(name string) (*Tag, error)
	// GetByUser(userID int) ([]*Tag, error)
	// Update(tag *Tag) error
	// Delete(id int) error
}
